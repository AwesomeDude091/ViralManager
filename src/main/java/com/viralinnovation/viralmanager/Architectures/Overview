Architecture Types:
1. Daemon (Interaction through Process)
2. JVM (Process)
3. Python (Process)
4. Docker
5. Shell/Bash (Interaction through Process)

   more possibilities

Architecture needs:
    in/out stream
    Port
    Start/Stop
    Restart
    *dependency resolution (universal)*

other:

    Services = basic (extendable with plugins)
    include additions (extend base functionality of website) in services